#include "stdafx.h"

#pragma once

#include "../ISystem.h"

namespace Engine::System {
	class GpuUploadSystem : public ISystem {
        void initialize(Scene::Scene& scene) {
        };
        void update(float dt) override {

        };
        void shutdown() override {
            
        };
	};
}